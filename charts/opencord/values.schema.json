{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": [
    "opencord",
    "image",
    "api",
    "realtime",
    "worker",
    "database",
    "valkey",
    "kafka",
    "scylla",
    "objectStorage",
    "livekit",
    "turn",
    "observability",
    "ingress",
    "customDomains"
  ],
  "properties": {
    "opencord": {
      "type": "object",
      "required": ["publicUrl", "environment"],
      "properties": {
        "publicUrl": { "type": "string", "minLength": 1 },
        "environment": { "type": "string", "minLength": 1 },
        "allowedOrigins": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 }
        }
      }
    },
    "image": {
      "type": "object",
      "required": ["registry", "tag", "pullPolicy"],
      "properties": {
        "registry": { "type": "string", "minLength": 1 },
        "tag": { "type": "string", "minLength": 1 },
        "pullPolicy": { "type": "string", "enum": ["Always", "IfNotPresent", "Never"] }
      }
    },
    "api": { "$ref": "#/definitions/workloadWithService" },
    "realtime": { "$ref": "#/definitions/workloadWithService" },
    "worker": { "$ref": "#/definitions/workloadWithPort" },
    "database": {
      "type": "object",
      "required": ["image", "external", "bundled"],
      "properties": {
        "image": {
          "type": "object",
          "required": ["repository", "tag"],
          "properties": {
            "repository": { "const": "timescale/timescaledb" },
            "tag": { "const": "2.28.1-pg18-oss" }
          }
        },
        "external": { "$ref": "#/definitions/externalUrlSecret" },
        "bundled": {
          "type": "object",
          "required": ["enabled", "servicePort", "auth", "persistence"],
          "properties": {
            "enabled": { "type": "boolean" },
            "servicePort": { "type": "integer", "minimum": 1, "maximum": 65535 },
            "auth": {
              "type": "object",
              "required": ["username", "database"],
              "properties": {
                "username": { "type": "string", "minLength": 1 },
                "database": { "type": "string", "minLength": 1 }
              }
            },
            "persistence": { "$ref": "#/definitions/persistence" },
            "resources": { "type": "object" }
          }
        }
      }
    },
    "valkey": {
      "type": "object",
      "required": ["external", "bundled"],
      "properties": {
        "external": { "$ref": "#/definitions/externalUrlSecret" },
        "bundled": {
          "type": "object",
          "required": ["enabled", "image", "servicePort", "persistence"],
          "properties": {
            "enabled": { "type": "boolean" },
            "image": {
              "type": "object",
              "required": ["repository", "tag"],
              "properties": {
                "repository": { "type": "string", "minLength": 1 },
                "tag": { "type": "string", "minLength": 1 }
              }
            },
            "servicePort": { "type": "integer", "minimum": 1, "maximum": 65535 },
            "persistence": { "$ref": "#/definitions/persistence" },
            "resources": { "type": "object" }
          }
        }
      }
    },
    "kafka": {
      "type": "object",
      "required": ["bootstrapServers"],
      "properties": {
        "bootstrapServers": {
          "type": "array",
          "minItems": 1,
          "items": { "type": "string", "minLength": 1 }
        }
      }
    },
    "scylla": {
      "type": "object",
      "required": ["contactPoints", "keyspace"],
      "properties": {
        "contactPoints": {
          "type": "array",
          "minItems": 1,
          "items": { "type": "string", "minLength": 1 }
        },
        "keyspace": { "type": "string", "minLength": 1 }
      }
    },
    "objectStorage": {
      "type": "object",
      "required": ["provider", "bucket", "existingSecret"],
      "properties": {
        "provider": { "type": "string", "enum": ["s3"] },
        "bucket": { "type": "string" },
        "endpoint": { "type": "string" },
        "existingSecret": { "type": "string" },
        "accessKeyIdKey": { "type": "string", "minLength": 1 },
        "secretAccessKeyKey": { "type": "string", "minLength": 1 }
      }
    },
    "livekit": {
      "type": "object",
      "required": ["enabled", "externalUrl", "mediaRegion", "mediaTokenTtlSeconds", "existingSecret"],
      "properties": {
        "enabled": { "type": "boolean" },
        "externalUrl": { "type": "string" },
        "mediaRegion": { "type": "string", "minLength": 1 },
        "mediaTokenTtlSeconds": { "type": "integer", "minimum": 60 },
        "existingSecret": { "type": "string" },
        "apiKeyKey": { "type": "string", "minLength": 1 },
        "apiSecretKey": { "type": "string", "minLength": 1 }
      }
    },
    "turn": {
      "type": "object",
      "required": ["enabled", "mode", "urls", "existingSecret", "realm", "relayPortRange"],
      "properties": {
        "enabled": { "type": "boolean" },
        "mode": { "type": "string", "enum": ["external", "bundled"] },
        "urls": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 }
        },
        "existingSecret": { "type": "string" },
        "realm": { "type": "string" },
        "usernameKey": { "type": "string", "minLength": 1 },
        "credentialKey": { "type": "string", "minLength": 1 },
        "sharedSecretKey": { "type": "string", "minLength": 1 },
        "relayPortRange": {
          "type": "object",
          "required": ["min", "max"],
          "properties": {
            "min": { "type": "integer", "minimum": 1, "maximum": 65535 },
            "max": { "type": "integer", "minimum": 1, "maximum": 65535 }
          }
        }
      }
    },
    "observability": {
      "type": "object",
      "required": ["otel", "log", "metrics"],
      "properties": {
        "otel": {
          "type": "object",
          "required": ["enabled", "endpoint", "serviceName"],
          "properties": {
            "enabled": { "type": "boolean" },
            "endpoint": { "type": "string" },
            "serviceName": { "type": "string", "minLength": 1 }
          }
        },
        "log": {
          "type": "object",
          "required": ["format", "filter"],
          "properties": {
            "format": { "type": "string", "enum": ["text", "json"] },
            "filter": { "type": "string", "minLength": 1 }
          }
        },
        "metrics": {
          "type": "object",
          "required": ["prometheusEnabled"],
          "properties": {
            "prometheusEnabled": { "type": "boolean" }
          }
        }
      }
    },
    "ingress": {
      "type": "object",
      "required": ["enabled", "hosts", "tls"],
      "properties": {
        "enabled": { "type": "boolean" },
        "className": { "type": "string" },
        "annotations": { "type": "object" },
        "hosts": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["host", "paths"],
            "properties": {
              "host": { "type": "string", "minLength": 1 },
              "paths": {
                "type": "array",
                "items": {
                  "type": "object",
                  "required": ["path", "service"],
                  "properties": {
                    "path": { "type": "string", "minLength": 1 },
                    "pathType": { "type": "string", "enum": ["Exact", "Prefix", "ImplementationSpecific"] },
                    "service": { "type": "string", "enum": ["api", "realtime"] }
                  }
                }
              }
            }
          }
        },
        "tls": { "type": "array" }
      }
    },
    "customDomains": {
      "type": "object",
      "required": ["enabled", "hosts", "tlsSecretName"],
      "properties": {
        "enabled": { "type": "boolean" },
        "hosts": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 }
        },
        "tlsSecretName": { "type": "string" }
      }
    }
  },
  "definitions": {
    "externalUrlSecret": {
      "type": "object",
      "required": ["enabled", "urlSecretName"],
      "properties": {
        "enabled": { "type": "boolean" },
        "urlSecretName": { "type": "string", "minLength": 1 }
      }
    },
    "persistence": {
      "type": "object",
      "required": ["enabled", "size", "storageClassName"],
      "properties": {
        "enabled": { "type": "boolean" },
        "size": { "type": "string", "minLength": 1 },
        "storageClassName": { "type": "string" }
      }
    },
    "workload": {
      "type": "object",
      "required": ["replicaCount", "image", "resources", "autoscaling"],
      "properties": {
        "replicaCount": { "type": "integer", "minimum": 0 },
        "image": {
          "type": "object",
          "required": ["repository"],
          "properties": {
            "repository": { "type": "string", "minLength": 1 }
          }
        },
        "resources": { "type": "object" },
        "autoscaling": { "$ref": "#/definitions/autoscaling" }
      }
    },
    "workloadWithService": {
      "allOf": [
        { "$ref": "#/definitions/workloadWithPort" },
        {
          "type": "object",
          "required": ["servicePort"],
          "properties": {
            "servicePort": { "type": "integer", "minimum": 1, "maximum": 65535 }
          }
        }
      ]
    },
    "workloadWithPort": {
      "allOf": [
        { "$ref": "#/definitions/workload" },
        {
          "type": "object",
          "required": ["containerPort"],
          "properties": {
            "containerPort": { "type": "integer", "minimum": 1, "maximum": 65535 }
          }
        }
      ]
    },
    "autoscaling": {
      "type": "object",
      "required": ["enabled", "minReplicas", "maxReplicas", "targetCPUUtilizationPercentage"],
      "properties": {
        "enabled": { "type": "boolean" },
        "minReplicas": { "type": "integer", "minimum": 1 },
        "maxReplicas": { "type": "integer", "minimum": 1 },
        "targetCPUUtilizationPercentage": { "type": "integer", "minimum": 1, "maximum": 100 }
      }
    }
  }
}
